define(["underscore", "backbone", "app/Node"], function (_, backbone, Node) {
    "use strict";

    return function () {
        describe("app/Node", function () {
            /*
            it("has traits of backbone.Model", function () {
                expect(_.isFunction(new Node({}, {window:window}).toJSON)).toBe(true);
            });
            it("has default properties", function () {
                expect(_.keys(new Node({}, {window:window}).defaults)).toEqual(["id", "king"]);
            });*/
        });
    };
});